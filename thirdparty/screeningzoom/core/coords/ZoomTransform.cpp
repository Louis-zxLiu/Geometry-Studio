#include "ZoomTransform.h"

namespace zoomit {
// All logic is header-inline; this TU exists so the build graph has a
// stable object file for the module and future out-of-line additions.
} // namespace zoomit
